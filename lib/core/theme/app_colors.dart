import 'package:flutter/material.dart';

class AppColors {
  // Primary
  static const Color primary      = Color(0xFF1CB0F6); // biru PERISAI
  static const Color primaryLight = Color(0xFF5ECDFB);
  static const Color primaryDark  = Color(0xFF0A8FD4);

  // Status
  static const Color danger       = Color(0xFFEF4444); // merah — judol terdeteksi
  static const Color success      = Color(0xFF10B981); // hijau — aman
  static const Color warning      = Color(0xFFF59E0B); // kuning — tidak aktif

  // Triggered by badge
  static const Color ocr          = Color(0xFF3B82F6); // biru
  static const Color mobilenet    = Color(0xFF8B5CF6); // ungu
  static const Color trustpositif = Color(0xFFEF4444); // merah
  static const Color combined     = Color(0xFFF97316); // oranye

  // Neutral
  static const Color background   = Color(0xFFF8FAFC);
  static const Color surface      = Color(0xFFFFFFFF);
  static const Color textPrimary  = Color(0xFF1E293B);
  static const Color textSecondary = Color(0xFF64748B);
  static const Color border       = Color(0xFFE2E8F0);
}